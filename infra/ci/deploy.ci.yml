# GitOps deploy jobs — enable as a GitHub Actions workflow job once a
# target registry / values repo is configured.
#
# .deploy:
#   runs-on: ubuntu-latest
#   env:
#     GIT_OPS_REPO: github.com/org/neuroatlas-deploy.git
#     GIT_OPS_BRANCH: master
#   steps:
#     - uses: actions/checkout@v4
#     - run: apt update -y && apt install -y yq git
#     - run: |
#         git clone --depth=1 "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@${GIT_OPS_REPO}" \
#           -b ${GIT_OPS_BRANCH} repo
#         cd repo
#         for SVC in $SERVICES; do
#           yq -yi ".image.tag = \"$IMAGE_TAG\"" "${ENVIRONMENT}/${SVC}/values.yaml"
#           git add "${ENVIRONMENT}/${SVC}/values.yaml"
#         done
#         git commit -m "Release ${IMAGE_TAG} to ${ENVIRONMENT} (${{ github.run_id }})"
#         git push origin ${GIT_OPS_BRANCH}
#
# deploy:dev:tag:
#   needs: [build-patients, build-ml, build-housekeeper]
#   if: startsWith(github.ref, 'refs/tags/')
#   env:
#     ENVIRONMENT: dev
#     IMAGE_TAG: ${{ github.ref_name }}
#     SERVICES: patients ml housekeeper
