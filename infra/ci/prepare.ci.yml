# Reserved for release-time jobs (e.g. changelog generation on semver tags).
# Add as a job in .github/workflows/ci.yml once scripts/changelog.py exists.
#
# changelog:
#   if: startsWith(github.ref, 'refs/tags/')
#   runs-on: ubuntu-latest
#   container:
#     image: python:3.12-slim-bookworm
#   steps:
#     - uses: actions/checkout@v4
#     - run: python scripts/changelog.py --tag "${{ github.ref_name }}"
